export interface LocationOptions {
    isTimeSlot: boolean;
    date: string;
    userIds: number[];
    devIds: number[];
    startTime: string;
    endTime: string;
    time: string;
    teamIds: number[];
    workTypeIds: number[];
}